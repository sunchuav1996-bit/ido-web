# ========================================
# API Gateway Configuration
# ========================================
# The base URL for all API calls (Lambda via API Gateway)
# Format: https://<api-id>.execute-api.<region>.amazonaws.com
# This is your API Gateway invoke URL (without trailing slash)
REACT_APP_API_BASE_URL=https://xxxxx.execute-api.us-east-1.amazonaws.com

# ========================================
# AWS Configuration (Only needed for S3/Photo uploads)
# ========================================
# AWS Region where your services are deployed
REACT_APP_AWS_REGION=us-east-1

# S3 bucket name for photo uploads
REACT_APP_S3_BUCKET_NAME=ido-user-photos

# ========================================
# DynamoDB Configuration
# ========================================
# Table name for storing contact messages
REACT_APP_CONTACT_MESSAGES_TABLE=ido-contact-messages

# Table name for storing orders
REACT_APP_DYNAMODB_TABLE_NAME=ido-orders
